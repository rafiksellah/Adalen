<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Adalen - Centre d'Activités Extra-Scolaires pour Enfants{% endblock %}</title>
    <meta name="description" content="Adalen - Un espace dédié à l'épanouissement des enfants à travers des activités extra-scolaires variées, ludiques et éducatives.">
    
    <!-- Favicon -->
    <link rel="icon" href="{{ asset('assets/img/logo.png') }}" type="image/png">
    
    <!-- Google Fonts - Poppins & Nunito -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Nunito:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    
    <!-- Custom CSS -->
    <link href="{{ asset('assets/css/adalen.css') }}" rel="stylesheet">
    
    {% block stylesheets %}{% endblock %}
</head>
<body>
    <!-- Navigation Moderne -->
    <nav class="navbar navbar-expand-lg navbar-light sticky-top">
        <div class="container">
            <a class="navbar-brand" href="{{ path('app_home') }}">
                <img src="{{ asset('assets/img/logo.png') }}" alt="Adalen Logo" class="navbar-logo">
                <span class="brand-text">Adalen</span>
            </a>
            <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'app_home' %}active{% endif %}" href="{{ path('app_home') }}">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'app_activities' %}active{% endif %}" href="{{ path('app_activities') }}">Activités</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'app_animators' %}active{% endif %}" href="{{ path('app_animators') }}">Nos Animateurs</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'app_contact' %}active{% endif %}" href="{{ path('app_contact') }}">Contact</a>
                    </li>
                    <li class="nav-item">
                        <a href="https://montessori-algeria.com/" target="_blank" class="nav-link nav-link-external" title="Aller au site Montessori">
                            <i class="fas fa-home me-2"></i>Montessori
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Flash Messages -->
    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert alert-{{ label == 'error' ? 'danger' : label }} alert-dismissible fade show" role="alert">
                {{ message }}
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        {% endfor %}
    {% endfor %}

    <!-- Main Content -->
    <main>
        {% block body %}{% endblock %}
    </main>

    <!-- Footer Moderne -->
    <footer class="footer">
        <div class="container">
            <div class="row g-4">
                <div class="col-lg-4 mb-4 mb-lg-0">
                    <div class="footer-brand">
                        <img src="{{ asset('assets/img/logo.png') }}" alt="Adalen Logo" class="footer-logo">
                        <span>Adalen</span>
                    </div>
                    <p>Un espace dédié à l'épanouissement des enfants à travers des activités extra-scolaires variées, ludiques et éducatives.</p>
                </div>
                <div class="col-lg-4 mb-4 mb-lg-0">
                    <h5>Liens Rapides</h5>
                    <ul>
                        <li><a href="{{ path('app_home') }}"><i class="fas fa-chevron-right"></i> Accueil</a></li>
                        <li><a href="{{ path('app_activities') }}"><i class="fas fa-chevron-right"></i> Activités</a></li>
                        <li><a href="{{ path('app_animators') }}"><i class="fas fa-chevron-right"></i> Nos Animateurs</a></li>
                        <li><a href="{{ path('app_contact') }}"><i class="fas fa-chevron-right"></i> Contact</a></li>
                    </ul>
                </div>
                <div class="col-lg-4">
                    <h5>Contact</h5>
                    <p>
                        <a href="tel:+213563000517"><i class="fas fa-phone"></i> +213 563 000 517</a>
                    </p>
                    <p>
                        <a href="mailto:montessoriadalen@gmail.com"><i class="fas fa-envelope"></i> montessoriadalen@gmail.com</a>
                    </p>
                    <p>
                        <a href="#"><i class="fas fa-map-marker-alt"></i> Hydra – Alger</a>
                    </p>
                </div>
            </div>
            <hr>
            <div class="footer-bottom">
                <p class="mb-0">&copy; {{ 'now'|date('Y') }} Adalen. Tous droits réservés.</p>
            </div>
        </div>
    </footer>

    <!-- Bootstrap 5 JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Custom JS -->
    <script src="{{ asset('assets/js/adalen.js') }}"></script>
    
    {% block javascripts %}{% endblock %}
</body>
</html>

